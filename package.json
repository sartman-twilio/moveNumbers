{
  "name": "moveNumbers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "2.1.0",
    "twilio": "^5.4.0"
  },
  "devDependencies": {
    "twilio-run": "^4.0.3"
  },
  "engines": {
    "node": "18"
  }
}
